export class UnrealBloomPass {
  constructor(size, strength, radius, threshold) {
    this.enabled = true;
    this.strength = strength;
    this.radius = radius;
    this.threshold = threshold;
    this.setSize = () => {};
    this.dispose = () => {};
  }
}
